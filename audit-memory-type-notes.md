Memory table allows memoryType values SITUATION, STRATEGY, DECISION, ACTION, OUTCOME, LEARNING, ASSUMPTION, PATTERN. Historical-context matching compares queryType to memoryType/sourceType and searches title, summary, contextJson. Strategy review can query STRATEGY; Action review can query ACTION. Daily Brief is a synthesis surface rather than a persisted memory type, so it should use an evidence-only panel with an explicit no-match fallback and link to the Memory workspace, without inventing a BRIEF memory category.
